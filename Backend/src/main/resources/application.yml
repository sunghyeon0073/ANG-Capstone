spring:
  application:
    name: Ang

  profiles:
    active: ${SPRING_PROFILES_ACTIVE:local}

  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: org.mariadb.jdbc.Driver
    hikari:
        maximum-pool-size: 10
        minimum-idle: 1
        idle-timeout: 30000
        max-lifetime: 1800000
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MariaDBDialect
    open-in-view: false

  servlet:
    multipart:
      enabled: true
      max-file-size: 50MB
      max-request-size: 100MB

jwt:
  secret: ${JWT_SECRET}
  access-token-expiration: 1800000
  refresh-token-expiration: 604800000

admin:
  init:
    password: ${ADMIN_INIT_PASSWORD}

cors:
  allowed-origin-patterns: ${CORS_ALLOWED_ORIGIN_PATTERNS:http://localhost:3000,http://localhost:5173,http://localhost:5500,http://127.0.0.1:3000,http://127.0.0.1:5173,http://127.0.0.1:5500,http://56.155.135.33:5500}

file:
  upload-dir: ${FILE_UPLOAD_DIR:uploads}

ai:
  base-url: ${AI_BASE_URL:http://localhost:8888}

ollama:
  base-url: ${OLLAMA_BASE_URL:http://localhost:11434}
  model: ${OLLAMA_MODEL:ang-ai:latest}
  timeout: 120

server:
  port: 9090
  servlet:
    context-path: /api

logging:
  level:
    root: info
    com.ang: debug
    org.hibernate.SQL: debug
    org.springframework.security: warn

cloud:
  aws:
    region: ap-northeast-3
    s3:
      bucket: ang-s3-bucket-418295679042-ap-northeast-3-an